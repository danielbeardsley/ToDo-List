<% @lists.each do |list| %>
  <div class="list_buttons_row">
  <%= link_to "Get an item from: #{list.name}", get_list_current_item_path(list), :method => :post %>
  <%= link_to "Add an item to: #{list.name}", new_list_item_path(list) %>
  </div>
<% end %>

<br/>

<%= link_to 'New list', new_list_path %>