<p>
  <b>Name:</b>
  <%=h @list.name %>
</p>

<p>
  <b>Current item:</b>
  <%= @list.current_item ? @list.current_item.title : "<span class='empty'>(none)</span>" %>
</p>

<p>
  <b>Items:</b>
  <table class="'item_table">
  <% @list.items.uncompleted.each do |item| %>
    <tr>
      <td><%=h item.title %></td>
      <td><%= link_to 'Show', item, :class => 'show' %></td>
      <td><%= link_to 'Complete', complete_item_path(item), :method => :post, :confirm => "Are you sure you've finished this?", :class => 'complete' %></td>
      <td><%= link_to 'Edit', edit_item_path(item), :class => 'edit' %></td>
      <td><%= link_to 'Delete', item, :confirm => 'Are you sure you want to delete this item?', :method => :delete, :class => 'delete' %></td>
    </tr>
  <% end %>
  </table>
</p>

<%= link_to 'New item', new_list_item_path(@list),:class => 'create' %>
<%= link_to 'Edit', edit_list_path(@list) %>
<%= link_to 'Back', lists_path %>