<h1>Listing items</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Due</th>
    <th>Last seen</th>
    <th>Type</th>
    <th>Completed</th>
    <th>Date completed</th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%=h item.title %></td>
    <td><%=h item.due %></td>
    <td><%=h item.last_seen %></td>
    <td><%=h item.type %></td>
    <td><%=h item.completed %></td>
    <td><%=h item.date_completed %></td>
    <td><%= link_to 'Show', item %></td>
    <td><%= link_to 'Edit', edit_item_path(item) %></td>
    <td><%= link_to 'Destroy', item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New item', new_item_path %>